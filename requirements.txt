# ============================================================================
# requirements.txt — 项目 Python 依赖清单
# ----------------------------------------------------------------------------
# 用途:声明本项目运行需要的所有第三方 Python 包及其版本
# 安装命令(下次对话才执行,今天只是写出来):
#     pip install -r requirements.txt
# 维护规则:每加一个新包,必须同步加到这里,并指定版本号
# 版本选择原则:用次新稳定版,锁死(==),保证可重现性
# ============================================================================

# --- Web 框架与服务器 ---
fastapi==0.136.1          # FastAPI:Python 现代异步 Web 框架,W4 用它做后端入口
uvicorn==0.46.0           # uvicorn:运行 FastAPI 的 ASGI 服务器(类比:Tomcat 之于 Java)

# --- 模板与表单 ---
jinja2==3.1.6             # Jinja2:HTML 模板引擎,W4 用它把数据塞进 HTML 页面
python-multipart==0.0.27  # 让 FastAPI 能处理表单/文件上传(W15 上传 Excel 用)

# --- Excel 数据读写 ---
openpyxl==3.1.5           # 读写 .xlsx 文件,W2 开始用,阶段 1 的核心数据访问库