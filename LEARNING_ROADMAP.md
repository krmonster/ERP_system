# LEARNING_ROADMAP.md — 医疗耗材验收系统全栈学习路线图

> 这是一份从 W1 到 W44 的**完整学习路线图**,覆盖从零基础到生产部署的全部旅程。
>
> **使用规则**:
> - 每周日花 15 分钟对照本文档,在 LEARNING_LOG.md 里写本周复盘
> - 实际进度允许偏离,但偏离 2 周以上要主动调整
> - 这不是 KPI,是地图。慢一点不丢人,断了不学才丢人

---

## 复盘与跨周机制(实操规则)

本路线图 44 周的执行,依赖一套"复盘 + 跨周"的工作流。这套工作流的目的:**让用户在不同对话之间无缝衔接,不出现项目断档和节奏错乱**。

### 一、用户主动触发的周复盘

触发词(任选其一):
- "周复盘"
- "本周复盘"
- "周总结"
- "本周总结"
- 任何包含"复盘""总结"且语境指向"过去这一周"的表达

AI 收到触发词后,自动执行:
1. 读取 LEARNING_LOG.md 最新一周记录
2. 对照本路线图当周预期
3. 给出三项结构化反馈:
   - **完成度**:实际 vs 计划的差距(用 🟢🟡🔴 标记)
   - **卡点诊断**:有 🟡🔴 时,定位原因(概念没懂?时间不够?状态低谷?)
   - **下周建议**:给具体可执行的调整方案
4. 起草新一周的 LOG 模板(包含本周记录),用户确认后输出完整 LOG 文本,供用户复制覆盖上传

### 二、AI 主动触发的跨周提醒

每次新对话开始时,AI 自动执行:
1. 读 LEARNING_LOG.md 最新一周记录
2. 检查"本周记录的日期范围"是否覆盖今天
3. 如果今天已超出最新一周记录范围,主动提醒"该复盘了"
4. 连续 3 次对话未更新 LOG,询问原因
5. 连续 4 周以上无学习活动,让用户决定"重启 vs 暂停"

### 三、用户主动触发的 AI 自检(关键)

这条规则是为了把流程响应的可靠性从 95% 提升到 99%+ 。

**用户的特权命令**(任何对话中都能用):

> "自检 Instructions 执行情况"
> 或简写:"自检"

AI 收到后,必须立刻停下当前任务,执行:
1. 回顾本对话最近 5-10 轮自己的回复
2. 检查是否有违反 Instructions 的地方,例如:
   - 是否漏做了"开场仪式"(读 LOG 给进度速览)
   - 是否在用户没确认的情况下连做了多步
   - 是否在不该用 Instructions 之外的技术(超出当前阶段)
   - 是否漏了"先讲概念再写代码"的教学顺序
3. 诚实输出:"我做对了 X,我漏了 Y,接下来怎么纠正"
4. 等用户确认后再继续原任务

**这条规则的意义**:用户用一句话,就能强制 AI 做一次诚实的自我审计,纠正悄悄发生的偏差。

### 四、工作流原则

1. **用户更新进度的唯一权威渠道是 LEARNING_LOG.md**(用户每周日自己维护)
2. 本路线图(ROADMAP)是地图,**不记录进度**,基本不更新
3. AI 的行为规则在 Project Instructions 里(短而硬,生效优先级最高),本章节只是说明这套机制存在,方便用户随时回看
4. **三份文件分工**:
   - Project Instructions:AI 的行为规则(用户在 Claude.ai 网页 Project 设置里维护)
   - LEARNING_ROADMAP.md:学习内容地图(几乎不变)
   - LEARNING_LOG.md:实际进度记录(每周更新)
   - CLAUDE.md:项目代码规范

### 五、关于规则可靠性的诚实说明

AI 不是程序,**不能 100% 严格执行写在文档里的规则**。即使把规则写得再清楚,实际响应仍可能出现偏差。这是当前 AI 的真实限度,不是机制设计问题。

正确的预期设定:
- **写在 Instructions 的短硬规则**:执行率约 95-97%(主防线)
- **本章节描述的"用户触发的自检"**:执行率约 99%(因为用户直接命令我审计自己,我必然会停下来做)
- **用户的"事后纠错"权**:执行率 100%(我跑偏时,用户提醒一句,我立刻回到正轨)

**三层叠加,实际可靠性 ≈ 99%+**。

不要把 99.99% 的期望寄托在"AI 自动完美执行"上。**AI 的可靠性 = AI 的努力 + 用户的主动权**。这才是和 AI 协作的真实工程思维。

---

## 学习者画像与时间假设

- **身份**:上班族,Python 基础不错,会 HTML/CSS,JS 入门
- **投入**:工作日每天 1.5h × 5 天 + 周末每天 2.5h × 2 = 周计划 12.5h
- **打折后真实投入**:每周约 9 小时(扣除加班、状态低谷、生活意外)
- **总长**:36-44 周,约 8-10 个月
- **目标**:不是速成,是真正掌握全栈,做出能用的生产系统

---

## 全局阶段总览

| 阶段 | 名称 | 周次 | 时长 | 累计 | 关键产出 |
|------|------|------|------|------|----------|
| 1 | 本地 Excel 作业台 | W1-W6 | 6 周 | M1.5 | 能扫码查询的本地网页 |
| 1.5 | SQLite 数据库训练 | W7-W10 | 4 周 | M2.5 | 数据库版后端,前端不变 |
| 2 | PostgreSQL 正式后端 | W11-W16 | 6 周 | M4 | 企业级数据库 + 数据建模 |
| **中场** | 复盘 + 整理 | W17-W18 | 2 周 | M4.5 | 知识沉淀,决定是否继续 |
| 3 | React + TypeScript | W19-W32 | 14 周 | M8 | 现代前端 + 组件化 |
| 4 | 生产部署 | W33-W38 | 6 周 | M9.5 | Docker + Nginx + HTTPS |
| **机动** | 缓冲 + 优化 | W39-W44 | 6 周 | M10 | 应对延期或额外打磨 |

⚠️ **W39-W44 是机动周**。如果前面有阶段超时(大概率发生),从这里借时间。

---

## 阶段 1:本地 Excel 作业台(W1-W6)

**核心使命**:把"前后端协作"从抽象概念变成肌肉记忆。

### W1:项目骨架 + 常量定义

**对应步骤**:1(项目骨架) + 2(constants.py)

**学习内容**:
- 虚拟环境 venv 是什么、为什么需要
- Python 包结构、`__init__.py` 的作用
- pip 和 requirements.txt 的关系
- 为什么固定字段要集中管理(常量集中化原则)

**工作日(每天 1.5h)**:
- 周一:概念学习(虚拟环境、依赖管理),不写代码
- 周二:跟 Claude 对话,理解项目目录结构的意义
- 周三:动手创建项目目录,装依赖
- 周四:开始写 constants.py,理解每个常量为什么存在

**周末(共 5h)**:
- 周六:写完 constants.py,跑通 `from app.constants import *`
- 周日:回看代码,自己复述每个常量的作用,准备 W2

**正反馈节点**:🎯 看见自己亲手搭出的项目目录树,Python 能成功导入常量

**潜在卡点**:虚拟环境激活在 Windows 下可能踩坑(执行策略限制),要 Claude 提前演示

---

### W2:日志 + Excel 读取层

**对应步骤**:3(logging_config) + 4(excel_order_repository.py)

**学习内容**:
- Python logging 的基础(级别、格式、handler)
- openpyxl 怎么打开 Excel、读表头、遍历数据行
- pathlib 处理路径,为什么不用相对路径
- 三层架构里 Repository 层的职责边界
- 异常处理(FileNotFoundError、ValueError)

**工作日**:
- 周一二:logging 概念 + 写小例子练手
- 周三四:openpyxl 文档跟着敲(用一个临时 test.xlsx 而不是主项目)

**周末**:
- 周六:写 logging_config.py + Repository 主体
- 周日:写一个 5 行的临时测试脚本,真实查询自己的 DD码

**正反馈节点**:🎯 命令行打印出 Excel 里真实的明细数据,**第一次"用代码读自己的业务数据"**

**潜在卡点**:Excel 里日期单元格读出来是 datetime 对象不是字符串,需要 Claude 演示转换

---

### W3:业务逻辑层 Service

**对应步骤**:5(order_service.py)

**学习内容**:
- 为什么要有 Service 层(分层架构的真正价值)
- 业务判断和数据访问的边界
- 返回结构标准化(success/found/message/data 的设计哲学)
- 三种边界情况:空输入、查不到、查到

**工作日**:
- 周一二:理解 Service 概念,看 Repository 和 Service 的区别例子
- 周三四:写 Service 主体

**周末**:
- 周六:测三种边界(空 DD码/不存在/查到),三种返回结构都对得上
- 周日:回看代码,把所有"为什么这么写"讲给自己听一遍

**正反馈节点**:🎯 写一个测试脚本,跑通三种业务分支,所有返回结构标准且统一

**潜在卡点**:容易把判断逻辑写到 main.py 或 Repository 里,Claude 需要随时纠偏

---

### W4:FastAPI 入口 ⚠️ 难周

**对应步骤**:6(main.py)

**学习内容**:
- HTTP 请求/响应的基本原理
- FastAPI 路由(@app.get、@app.post)
- 路径参数 vs 查询参数
- Jinja2 模板渲染
- 静态文件挂载
- 异常捕获和结构化错误返回

**工作日策略**:
- 周一:看 FastAPI 官方教程的"First Steps",**不动主项目**
- 周二:跟 Claude 写一个最小 hello world(10 行代码)
- 周三:写一个返回 JSON 的接口
- 周四:写一个渲染 HTML 模板的接口

**周末(集中攻坚)**:
- 周六上午:把工作日学的概念再过一遍
- 周六下午+周日:写 main.py 完整版

**正反馈节点**:🎯 浏览器访问 `http://127.0.0.1:8000/api/order?dd_code=xxx` 拿到 JSON 数据

**潜在卡点**:
- uvicorn 启动报错(模块路径问题)
- 静态文件 404(挂载路径配错)
- 这周如果周末没搞定,**允许花一整个 W5 处理 W4**,不要硬推

---

### W5:HTML + JS + CSS ⚠️ 难周

**对应步骤**:7(index.html) + 8(style.css)

**学习内容**:
- DOM(文档对象模型)、querySelector、innerHTML
- 事件处理(click、keydown、Enter 触发)
- fetch 函数的使用
- async/await 异步编程
- JSON 解析
- CSS 基本布局(居中、表格样式、提示色)

**工作日策略**:
- 周一:DOM 操作概念 + 小例子(纯 HTML+JS,不接后端)
- 周二:fetch + async/await,写一个调公开 API 的小例子
- 周三:把小例子和后端连起来跑通
- 周四:研究 index.html 的结构

**周末**:
- 周六:写 index.html 主体 + JS 逻辑
- 周日:写 CSS 样式 + 联调

**正反馈节点**:🎉 **大正反馈**:浏览器输入真实 DD码 → 点查询 → 表格出现数据

**潜在卡点**:
- fetch 跨域问题(本地一般不会有,有的话立刻问 Claude)
- 表格渲染顺序错乱(JS 数据流没理清)
- DOM 元素 id 拼写错误导致 JS 找不到节点

---

### W6:联调 + 第 2 天写回功能

**对应步骤**:9(联调验证) + 第 2 天(POST 写回)

**学习内容**:
- HTTP GET vs POST 的语义差异
- Excel 写回(openpyxl 的保存机制)
- 风险:并发写入、数据备份
- 完整闭环测试

**工作日**:
- 周一:整理已有代码,加注释
- 周二:学 POST 接口怎么写
- 周三四:写 POST /api/order/accept 后端逻辑

**周末**:
- 周六:加前端"整单验收"按钮 + 调用 POST
- 周日:**完整闭环测试** + 写阶段 1 总结

**正反馈节点**:🎉🎉 **阶段终点**:扫码 → 查询 → 点击验收 → 状态从"未验收"变"已验收" → 整个闭环

**阶段 1 验收清单**:
- [ ] 服务能用 `python -m uvicorn` 启动无 ERROR
- [ ] 浏览器查询 + 整单验收都能跑通
- [ ] 三层架构干净:main 不读 Excel、Service 不读 Excel、只有 Repository 读
- [ ] constants.py 里集中了所有固定字段
- [ ] 你能用 5 句话讲清楚:用户点查询按钮后,数据走过了哪几层

---

## 阶段 1.5:SQLite 数据库训练(W7-W10)

**核心使命**:从"文件思维"过渡到"数据库思维"。这是最关键的认知跃迁。

### W7:SQL 基础

**学习内容**:
- 关系型数据库的核心概念(表、行、列、主键)
- SQLite 命令行工具
- SELECT 语句(WHERE、ORDER BY、LIMIT)
- INSERT、UPDATE、DELETE
- 数据类型(TEXT、INTEGER、DATE)

**工作日**:每天 1.5h 看 SQL 教程 + 在 SQLite 命令行手敲

**周末**:
- 周六:在 SQLite 里手动建一张 orders 表,插几行数据
- 周日:用 SELECT 查到 DD码 数据

**正反馈节点**:🎯 用 SQL 查到自己的业务数据,**意识到"数据库就是一个能用语言查询的 Excel"**

---

### W8:Excel 导入 SQLite

**学习内容**:
- 写一次性脚本,把 Excel 全部数据导进 SQLite
- 表结构设计(列名、类型、约束)
- Python 的 sqlite3 标准库

**工作日**:学 sqlite3 库的 connect/cursor/execute 模式

**周末**:
- 周六:写导入脚本
- 周日:验证数据完整性,SQL 查询数据 = Excel 数据

**正反馈节点**:🎯 一行命令把 Excel 整张表搬进数据库

---

### W9:SQLAlchemy ORM 改写 Repository ⚠️ 关键周

**学习内容**:
- ORM(对象关系映射)是什么、为什么需要
- SQLAlchemy 的 Engine、Session、Model
- 把现有 Repository 从 openpyxl 改写成 SQLAlchemy
- 前端代码完全不动

**工作日**:理解 ORM 概念 + 跟着 SQLAlchemy 教程做小例子

**周末**:
- 周六:写新版 Repository(基于 SQLAlchemy)
- 周日:**前端原封不动,启动服务测试**

**正反馈节点**:🎉🎉 **大正反馈**:页面行为完全一致,但底层换成了数据库。**这一刻请你停下来 5 分钟,体会"分层架构"的全部意义**

---

### W10:增强查询 + 阶段总结

**学习内容**:
- 给查询加条件(状态筛选、日期范围)
- 复合查询(WHERE + ORDER BY 组合)
- SQL 性能初探(索引概念,不深入)

**工作日**:学 WHERE 复合条件、ORDER BY 多列

**周末**:
- 周六:网页加"只看未验收"筛选按钮 + 后端支持 status 参数
- 周日:**阶段 1.5 总结**

**正反馈节点**:🎯 网页有了真正的"筛选"功能 —— 这在 Excel 时代是做不到的

**阶段 1.5 验收清单**:
- [ ] 数据库里有完整的订单数据
- [ ] Repository 完全用 SQLAlchemy,看不到 openpyxl
- [ ] 前端代码和阶段 1 几乎一样
- [ ] 你能用自己的话解释 ORM 是什么、解决了什么问题

---

## 阶段 2:PostgreSQL 正式后端(W11-W16)

**核心使命**:从"玩具数据库"过渡到"生产数据库"。

### W11:PostgreSQL 入门

**学习内容**:
- PostgreSQL 安装(Windows/Mac)
- pgAdmin 或 DBeaver 客户端
- psql 命令行
- PostgreSQL 与 SQLite 的差异(数据类型、SQL 方言、并发模型)

**周末验证**:🎯 在 psql 里建表、插数据、查询

---

### W12:从 SQLite 迁移到 PostgreSQL

**学习内容**:
- SQLAlchemy 切换数据库连接字符串
- 数据迁移脚本
- 类型差异处理

**周末验证**:🎯 后端连 PostgreSQL,页面照常工作 —— **再次体会 SQLAlchemy 的抽象红利**

---

### W13:Alembic 迁移工具

**学习内容**:
- 数据库版本管理的概念
- Alembic 初始化、生成迁移、应用迁移
- "代码即数据库版本"的工作流

**周末验证**:🎯 改一次表结构(加一列),Alembic 自动生成迁移脚本

---

### W14:数据建模(供应商表)

**学习内容**:
- 实体关系建模(ER 图)
- 外键、JOIN 查询
- 一对多关系建模

**周末验证**:🎯 设计供应商表,关联到订单表,跑通带 JOIN 的查询

---

### W15:Excel 导入功能(降级)

**学习内容**:
- FastAPI 文件上传
- 把 Excel 上传后自动入库
- 数据校验(列名、类型、必填)
- Excel 从"主数据源"降级为"导入工具"

**周末验证**:🎯 网页加上"上传 Excel"按钮,文件 → 入库 → 查询

---

### W16:错误处理 + 阶段总结

**学习内容**:
- 上传错误格式 Excel 的处理
- 业务错误码设计
- 日志记录优化

**周末**:
- 周六:加完整的错误处理
- 周日:**阶段 2 总结**

**阶段 2 验收清单**:
- [ ] PostgreSQL 跑着完整业务数据
- [ ] Alembic 管理数据库版本
- [ ] 至少有 2 张表,有外键关系
- [ ] Excel 已降级为导入工具
- [ ] 整个后端是真正可用的中等规模业务系统

---

## ⚠️ 中场休息(W17-W18)

走到这里,你已经拥有**一个完整可用的后端系统**。**强制停 2 周**,做三件事:

### W17:代码回顾

- 重读自己写的所有代码
- 把当时硬抄过去的部分弄懂
- 重写一两个写得最丑的函数

### W18:学习总结 + 决策

- 写一份 1500-2000 字的总结(发给自己,或写技术博客)
- 列出最大的收获和最大的挫败
- **决定要不要继续阶段 3**

🎯 **重要事实**:很多公司团队的内部系统就停在阶段 2。HTML+CSS+JS+FastAPI+PostgreSQL 已经能扛住中等规模业务。**如果时间精力不够,这是体面的成功**。

---

## 阶段 3:React + TypeScript(W19-W32)⚠️ 最难

**核心使命**:前端思维从"操作 DOM"转成"声明 UI 状态"。这是整个项目最大的坡。

### W19:TypeScript 基础(上)

**学习内容**:
- 为什么需要类型(JS 的类型陷阱)
- 基础类型、interface、type
- 函数类型、可选参数

**周末验证**:给阶段 1 的 JS 代码加上类型,**编辑器开始报错提示**

---

### W20:TypeScript 基础(下)

**学习内容**:
- 泛型基础
- 联合类型、交叉类型
- 类型推断

**周末验证**:把后端 API 的返回结构定义成 TS interface

---

### W21:React 核心概念(上)

**学习内容**:
- 组件思想
- JSX 语法
- props 传递

**周末验证**:🎯 写一个 hello world 组件,理解"声明式"

---

### W22:React 核心概念(下)

**学习内容**:
- useState
- 事件处理
- 受控组件(input)

**周末验证**:实现一个计数器 + 一个输入框

---

### W23:useEffect + fetch

**学习内容**:
- useEffect 的运行时机
- 用 React 调后端 API
- 异步状态管理(loading/error/data)

**周末验证**:🎯 用 React 调通后端 API,显示真实数据

---

### W24:⚠️ 缓冲周(必须保留)

**这周不学新东西**,做三件事:
- 重写 W19-W23 的所有例子
- 把不懂的回去补
- 给自己 5 分钟讲清楚:React 的"声明式"和原生 JS 的"命令式"差别

**为什么必须有这周**:阶段 3 的失败大多来自"硬推"。这周省了,W25 一定崩。

---

### W25:Vite 工程化

**学习内容**:
- 现代前端构建工具的作用
- Vite 项目初始化
- npm、package.json、node_modules

**周末验证**:`npm run dev` 启动项目,`npm run build` 出生产包

---

### W26:React Router(路由)

**学习内容**:
- 单页应用的概念
- 路由配置
- 多页面切换

**周末验证**:网页有 2 个页面,可以切换

---

### W27:AG Grid 入门

**学习内容**:
- AG Grid Community 安装
- 基础表格、列配置
- 数据绑定

**周末验证**:🎯 18 列表格在网页上展示

---

### W28:AG Grid 进阶

**学习内容**:
- 排序、筛选、分页
- 单元格格式化
- 列固定、列宽调整

**周末验证**:🎯 18 列表格秒级响应排序筛选

---

### W29:整合(上)— 重写查询页

**学习内容**:
- 把阶段 1 的查询页用 React 重写
- 状态管理整理

**周末验证**:React 版查询页跑通

---

### W30:整合(下)— 重写所有页面

**学习内容**:
- 整单验收页的 React 版
- 数据导入页的 React 版

**周末验证**:🎉 **大正反馈**:全套 React 版上线,旧 HTML 版退役

---

### W31:优化 + 用户体验

**学习内容**:
- Loading 动画、错误提示
- 表单校验
- 快捷键支持(扫码场景)

**周末验证**:整体使用体验明显比阶段 2 提升

---

### W32:阶段 3 总结

**周末**:
- **阶段 3 验收**
- 写阶段总结

**阶段 3 验收清单**:
- [ ] React 版前端取代旧 HTML 版
- [ ] AG Grid 表格能排序筛选
- [ ] TypeScript 类型覆盖完整
- [ ] 你能讲清楚:React 的"声明式"是什么意思

---

## 阶段 4:生产部署(W33-W38)

**核心使命**:让系统离开你的电脑,跑在真服务器上。

### W33:Docker 基础

**学习内容**:
- 容器化的概念
- Dockerfile 编写
- 给后端打镜像

**周末验证**:🎯 `docker run` 启动后端

---

### W34:docker-compose

**学习内容**:
- 多容器协作
- 后端 + 数据库 + 前端的组合
- 网络和数据卷

**周末验证**:🎯 一行命令拉起整套系统

---

### W35:Nginx 反向代理

**学习内容**:
- Nginx 基本配置
- 静态文件 + API 代理
- 一个域名同时服务前后端

**周末验证**:🎯 一个域名访问整套系统

---

### W36:HTTPS 证书

**学习内容**:
- Let's Encrypt 免费证书
- HTTPS 原理
- 证书自动续期

**周末验证**:🎯 浏览器锁形图标

---

### W37:Redis 缓存

**学习内容**:
- 缓存的价值
- Redis 基础命令
- 给热点查询加缓存

**周末验证**:🎯 常用查询测速对比,提速 5-10 倍

---

### W38:真实部署 + 项目终点

**学习内容**:
- 选一个轻量级 VPS(阿里云、腾讯云轻量服务器)
- 域名解析
- 服务器初始化、安全加固

**周末验证**:🎉🎉🎉 **最终正反馈**:发个真实网址给朋友/同事,他能在自己电脑上访问你做的系统

**阶段 4 验收清单**:
- [ ] 整套系统跑在云服务器上
- [ ] HTTPS 访问
- [ ] 你能讲清楚:从浏览器到数据库,请求经过哪些组件

---

## 机动周(W39-W44)

这 6 周是缓冲。可能的用途:

- 前面阶段超时(大概率,不丢人)
- 修 bug、加功能
- 写完整文档
- 录演示视频
- 准备分享给同行

如果前面没超时,这 6 周可以:
- 学一个新技术(测试、CI/CD)
- 做第二个小项目巩固
- 给当前项目加新模块(报量、OCR 等更高级功能)

---

## 重要的话(每月读一次)

**1. 时间会拉长,允许它拉长**
44 周可能变成 60 周。**只要不断,就还在路上**。

> **附:看不见的进度也是进度**
> 有些周表面"只完成了 3 个文件"或"只做了 2 件事",但其实吸收了概念、修了工具坑、建立了思维习惯。这些"看不见的进度"也算数,**不要因为可见成果列表短就觉得这周亏了**。学习的真实产出 = 代码 + 概念吸收 + 心智模型建立 + 踩坑经验,后三者从来不在 commit 历史里。

**2. 阶段 3 是真坎**
W19-W24 大概率会想放弃。**这不是你笨,是 React 真的反直觉**。提前知道,到时候不会觉得是自己的问题。

**3. 终点在哪儿,你说了算**
- 走完 W6 → 你超过了 90% 业余学编程的人能做出的成果
- 走完 W16 → 你拥有完整后端能力
- 走完 W38 → 你拥有真正可上线的全栈能力

**每个阶段都是合格终点。不必走完才算成功。**

**4. 正反馈是工具,用起来**
每个 🎯 节点完成时:
- 录 30 秒视频
- 截图
- 发给朋友
- 写一句话总结

不是矫情,是给大脑发奖励信号。**让"学习"和"愉悦"绑在一起,这才是可持续**。

**5. 自检是你的特权,大胆用**
任何时候你觉得我跑偏了、节奏不对、教学顺序乱了,**直接说"自检"**。这是你提升我可靠性的最强武器。不用客气,不用解释,一句话就够。

**6. AI 给的具体值,默认要验证**

AI(包括我)在以下三类内容上不可靠,看到必查:

- **版本号**(Python 包、Node 包等):训练数据有截止日,版本号会过时
- **链接、URL**:可能失效或变更
- **最新 API 用法、命令行选项**:框架在升级,旧的写法可能不再工作

验证方法:
- 让 AI 用 web 搜索一下当前最新稳定版
- 自己去官方文档 / PyPI / npm 查
- 看到具体数字、链接、命令,**先停半秒,问"这是哪一年的?"**

这不是不信任 AI,是工程师的基本动作。**质疑一次,省半年后的踩坑时间**。

附带一条:**UI 看起来不对 ≠ 功能真的错**。先验证功能(能跑吗?报错吗?),再担心样子。比如某个文件图标不是预期的颜色,可能只是因为依赖工具(如 Git)还没装,不是文件本身有问题。

---

**END OF LEARNING_ROADMAP.md**